# Runtime packages used by this assignment's Django API.
# The original file listed a much larger internal stack (jupyter, pandas,
# celery, matplotlib, ...). pandas==1.1.2 has no aarch64 wheels, so pip
# compiles it from source and gcc is OOM-killed. None of those packages
# are imported by the TODO app.
Django==3.0.5
django-cors-headers==3.6.0
djangorestframework==3.12.2
pymongo==3.11.2
